name: art_drop_finder
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.10.3

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  appwrite: 20.3.2
  geolocator: ^14.0.2
  getwidget: ^4.0.0
  image: ^4.1.7
  image_picker: ^1.1.2
  flutter_map: ^6.1.0
  latlong2: ^0.9.1
  map_launcher: ^4.4.2
  path_provider: ^2.1.5
  qr_flutter: ^4.1.0
  shared_preferences: ^2.3.2
  url_launcher: ^6.3.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
